const sum = (first, second) => {
  let result = 0;
  if(Number(first) && Number(second)) {
    console.log(result = first + second);
  } else {
    return console.log('Los valores no son correctos')
  }
};

sum ('', '');