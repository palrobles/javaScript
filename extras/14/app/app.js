const calculateAgeDays = (year, mounth, day) => {
  const yearNow = getFullYear();
  const dayYear = 365;

  let calculateYearAge = yearNow - year;
  let calculateMounthAge = (calculateYearAge * mountYear) - mounth;


};